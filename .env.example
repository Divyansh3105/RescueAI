# Copy to .env for docker compose. Never commit .env (RULES.md -> Security Rules).
POSTGRES_USER=rescueai
POSTGRES_PASSWORD=change-me
POSTGRES_DB=rescueai
# The deployed domain. Leave as localhost until the domain is decided.
SITE_ADDRESS=localhost
